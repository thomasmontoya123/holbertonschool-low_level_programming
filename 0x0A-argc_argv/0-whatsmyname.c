#include <stdio.h>
/**
 * main - entry
 * @argc: number of command args
 * @argv: string of commands
 *
 * return: 0
 */

int main (int argc, char * argv[])
{
 	(void) argc;
	printf ("%s\n", argv[0]);
	return(0);
}
