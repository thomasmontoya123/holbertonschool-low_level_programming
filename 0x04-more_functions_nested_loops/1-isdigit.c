#include "holberton.h"
/**
 *  * _isdigit - finds for digits
 *   * @c: integer value for the function
 *   (* a blank line
 *    * Description: Longer description of the function)?
 *    (* section header: Section description)*
 *     * Return: digitr  return 1
 *
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')

	return (1);

else

	return (0);

}
