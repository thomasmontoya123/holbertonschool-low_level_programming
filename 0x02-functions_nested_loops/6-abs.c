#include "holberton.h"
/**
 *  * _abs - absolute value of an integer
 *   * @a: integer value for the function
 *   (* a blank line
 *    * Description: Longer description of the function)?
 *    (* section header: Section description)*
 *     * Return: the value itself without signs
 *
 */
int _abs(int a)
{
if (a < 0)
{
a = a * -1;
return (a);
}

else
{
return (a);
}

}
