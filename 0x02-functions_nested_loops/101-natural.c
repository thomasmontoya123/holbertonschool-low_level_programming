#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	int a = 3;
	int sum = 0;

	do {
		if ((a % 3 == 0) || (a % 5 == 0))
			sum = sum + i;

		a++;
	} while (a < 1024);

	printf("%i\n", sum);
	return (0);
}
